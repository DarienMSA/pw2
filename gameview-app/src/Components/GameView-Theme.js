import { createTheme } from "@mui/material";


const btheme = createTheme({
  palette: {
    primary: {
      main: '#673ab7',
    },
    secondary: {
      main: '#ab6116',
    },
    info: {
      main: '#cddc39'
    }
  },
});

export default btheme;

